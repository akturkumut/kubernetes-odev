apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: test-pvc
  namespace: rook-ceph
spec:
  storageClassName: rook-ceph-block
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
